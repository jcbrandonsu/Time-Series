The electricity data. 
To create folders in a GitHub repository that appear like the ones in your screenshot, follow these steps:

1. Create a New Folder via GitHub Website
Navigate to your repository on GitHub.
Click on the "Add file" button and select "Create new file".
In the file name field, type the name of the folder you want to create followed by a / (slash). For example, if you want to create a folder named data, type data/.
After the slash, type a placeholder file name (e.g., .gitkeep or README.md).
Add some content to the file or leave it blank.
Commit the new file to create the folder.
Example:

To create a folder named data, type data/.gitkeep in the file name field.
Add a commit message and click "Commit new fi

